{
  "name": "brooklynbowl-waiter",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:android": "capacitor sync android",
    "build:android": "npm run sync:android && cd android && ./gradlew assembleDebug"
  }
}
